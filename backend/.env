DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=certicheck
JWT_SECRET=a_long_random_string
PORT=5000
NODE_ENV=development

# Solana
SOLANA_ENABLE=true
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet
CERTIFICATE_PROGRAM_ID=
# Optional: private key for a funded issuer wallet (base58) if you want server-side signing

# IPFS (Pinata)
PINATA_JWT=your_pinata_jwt       # or leave empty to use fallback
